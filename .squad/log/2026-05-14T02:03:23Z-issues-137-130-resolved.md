# Session Log: Issues #137 + #130 Resolved

**Timestamp:** 2026-05-14T02:03:23Z  
**Focus:** Convergent fix story + #135/#136 plan answers

---

## Issues Resolved

### #137: Preview SDK Pinning & Eval Failures
**Status:** ✅ CLOSED (verified in production)

Root cause: FoundryAgent signature drift. SDK 1.2.2 does not accept `model=` as a direct kwarg; must use `default_options={"extra_body": {"model": ...}}`.

**Unified fix applies to:**
- account-opening-service (4 call sites)
- ai-service (3 call sites)

**Verification:** Both pods now succeed end-to-end. Eval runs (previously 400 "Missing required parameter: 'model'") now complete.

### #130: Multi-Pod Counter "AI Calls Today" = 0
**Status:** ✅ CLOSED (verified in production)

The "AI Calls Today" counter was stuck at 0 because `FoundryAgent.run()` was failing silently. Every risk assessment fell through to the exception handler, skipping the counter increment.

**Same unified fix resolves it:** Counter now increments on each successful risk assessment.

---

## Prevention

1. **Pytest contract tests** added to both services — runtime introspection asserts no `model=` is passed as a direct kwarg
2. **Skill enhancement** — `.squad/skills/foundry-eval-debugging/SKILL.md` now has Rung 0 (FoundryAgent constructor contract check)
3. **Pin guard already in place** — `.github/workflows/preview-sdk-pin-guard.yml` ensures no future pin drift

---

## #135/#136 Planning Complete

Danny's unified plan for account-opening workflow persistence and customer-facing status is finalized. Brian answered 3 open questions:

1. **Provisioning stage:** Promoted to first-class pipeline tile
2. **Resubmit access:** Owner + admin, ERROR outcomes only (not DECLINE)
3. **Privacy review:** Required before customer-facing decline reasons ship; gate PR-5

**Unblock status:** Basher and Linus can now execute the 5-PR breakdown immediately.

---

## Next Steps

- Merge all decision inbox files into decisions.md
- Update cross-team history files to reflect fix scope
- Archive older decisions.md entries (if >20KB)
- Commit .squad/ changes
