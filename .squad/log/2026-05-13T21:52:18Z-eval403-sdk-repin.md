# Session Log: eval-403 SDK Repin

**Date:** 2026-05-13  
**Duration:** Background task completed  
**Key Decision:** Exact-pin preview-channel Azure AI SDKs (agent-framework-core/foundry 1.2.2, azure-ai-inference 1.0.0b9)  

## RCA Summary

Eval pipeline failed with 403/UnauthorizedUserAction on container rebuild. Investigation traced to unpinned preview SDK constraints in pyproject.toml (`agent-framework-* = "*"`), which resolved to PyPI 1.3.0 published 2026-05-08. 

Breaking changes in 1.3.0 altered eval request construction → raiserv rejected with 403. Rollback to 1.2.2 restored functionality.

## Applied Changes

- src/ai-service/pyproject.toml: agent-framework-core/foundry pinned to 1.2.2
- src/chatbot-service/pyproject.toml: same pins
- src/account-opening-service/pyproject.toml: same pins
- Commit 0b6255a staged (not pushed)
- GH issue #137 filed

## Convention

**Preview-channel SDK versioning rule:** Exact pins required (e.g., `"1.2.2"`, not `"*"` or ranges) due to lack of semver stability between beta releases. Stable dependencies continue using range constraints per repo standard.

## Future Work

- Add pre-commit lint to block agent-framework wildcard constraints
- Enable Dependabot with explicit upgrade PRs for preview SDKs
- Require eval smoke-test before merging agent-framework version bumps
