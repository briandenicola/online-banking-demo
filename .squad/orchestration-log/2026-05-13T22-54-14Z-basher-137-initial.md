# Orchestration Log: basher-137 (Initial SDK Pinning Attempt)

**Timestamp:** 2026-05-13T22:54:14Z  
**Agent:** basher-137  
**Model:** claude-opus-4.7  
**Mode:** background  
**Status:** COMPLETED  

## Summary

Initial attempt to fix issue #137 (Eval-403 from unpinned agent-framework preview SDKs).

## Work Done

- Verified preview-SDK pins were intact across `ai-service/chatbot-service/account-opening-service`
- Added CI guard `.github/workflows/preview-sdk-pin-guard.yml`
- Added `task lint:preview-sdk-pins` to Taskfile.lint.yml
- Verified guard passes against current tree with all three services exact-pinned
- Committed as 45e5fac

## Outcome

**Did NOT solve the 403.** Pinning was necessary but not sufficient. Brian revealed the pins had already shipped in previous commits and the eval still failed. Coordinator stopped this agent mid-flight and re-spawned as basher-137b to investigate the real root cause.

## Files Modified

- `.github/workflows/preview-sdk-pin-guard.yml` (created)
- `Taskfile.lint.yml` (added lint:preview-sdk-pins task)
- Various `pyproject.toml` files (reformatted, no functional change)

## Decision Captured

Decision stored in `.squad/decisions/inbox/basher-137-preview-sdk-pinning.md` (exact-pinning policy for preview SDKs + CI guard).
