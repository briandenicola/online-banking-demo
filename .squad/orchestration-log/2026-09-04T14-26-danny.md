# Orchestration Log: Danny — 2026-09-04T14:26:00Z

## Session: Banker Copilot epic — final ruling round + vocabulary reconciliation

**Task:** Record Brian's four final rulings (Q2, Q3, Q4, expired collapse)

**Duration:** Session turn

**Outcome:** SUCCESS

### Work Completed

- Recorded Q2 ruling: `payloadHash` display is PERMANENT. Requirement: must appear on approval read model, not merely server-side storage.
- Recorded Q3 ruling: denial reasons REQUIRED, minimum 20 characters, server-side validated. Applies to `HUMAN_DENIED` only; other three reasons are machine-generated.
- Recorded Q4 ruling: **NO**. Banker's own second signature never suffices at L2, MFA included. Separation of duties means separation of people. Recorded reasoning with category confusion correction (MFA answers "who", SoD answers "how many").
- Recorded collapsed lifecycle: no `expired` state. `proposed → pending → signed → executed`, `denied` as single terminal rejection state differentiated by four-value `terminalReason` enum.
- Clarified that TTL sweeper mechanism unchanged (still runs), but now writes `denied` + `TTL_EXPIRED` instead of `expired` state.
- Added explicit call-out that expiry still means denied, never auto-approved (silence is not consent).
- Identified subtle failure mode: `COUNT(*) WHERE status = 'denied'` now over-reports agent rejection by absorbing all timed-out proposals. §5.1.1(c) grouping rule pays for this; TTL_EXPIRED must be named explicitly in grouping.
- Clarified audit events stay differentiated: `ApprovalExpired` remains its own event type even though state merged.

### Artifacts Produced

`docs/epics/banker-copilot.md`:
- §5.1 — call-out on expiry semantics + grouping rule dependency
- §5.1.1 — four conditions section updated
- §5.3, §5.4.1, §5.4.2, §5.5, §5.7, §8.0, §9, §10 — lifecycle updates
- Risk 4 rewritten: idempotency key support becomes delivery dependency

Decision inbox: `danny-final-rulings.md`

Updated epic #332 body with ZERO OPEN QUESTIONS status

### Dependencies & Next Steps

- Turk to apply to policy engine design (implementation of all four rulings)
- Linus to handle Q2 (`payloadHash` on UI read model) and Q3/Q4 UI implications
- Risk 15 visibility: four-layer defence currently 1.5 layers until #334, #336 land

---

**Related Issues:** #332

**Related Files:**
- docs/epics/banker-copilot.md
- .squad/decisions/inbox/danny-final-rulings.md
