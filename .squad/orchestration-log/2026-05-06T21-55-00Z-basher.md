# Orchestration Log: Basher (Backend Dev)
**Session Timestamp:** 2026-05-06T21-55-00Z

## Task: Remove Redis Pod and Wire Managed Redis

**Mode:** Background  
**Status:** Completed

### Outcome
- **Deleted** `deploy/kustomize/base/redis.yaml` (in-cluster Redis pod + service)
- **Updated** `deploy/kustomize/base/kustomization.yaml` to remove `redis.yaml` resource reference
- **Updated** `deploy/kustomize/base/configmap.yaml` with placeholders for Azure Managed Redis:
  - `REDIS_HOST`: placeholder for Terraform output
  - `REDIS_PORT`: 10000 (Managed Redis port)
  - `REDIS__CONNECTIONSTRING`: placeholder with `ssl=True,abortConnect=False`
- **Updated** `docs/deployment-azure.md` with Managed Redis connection details and `az` commands
- **Preserved** `docker-compose.yml` (local dev Redis on 6380→6379)

### Follow-up Flagged
- **Entra ID Auth Implementation:** All services require SDK changes to support token-based auth:
  - .NET: `Microsoft.Azure.StackExchangeRedis` package
  - Python: `azure-identity` token provider
  - Go: `azidentity` token credential
- **Current state:** Services connect without authentication; will fail against Managed Redis

### Technical Notes
- ConfigMap placeholders must be replaced during deployment via Kustomize overlay/sed/script
- Each service hardcodes fallback to `redis:6379` — clarify these shouldn't be used in cloud
- Balanced_B0 requires TLS (always on) — ensure client configs set `ssl=true`
