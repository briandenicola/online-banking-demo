# Orchestration Log — Rusty — 2026-09-09

**Agent:** Rusty (Platform/Infra) · **Status:** Seeder fix merged, all four call sites corrected, Brian's reseed unblocked.

## Work Delivered

**Files Modified:**
- `scripts/demo/demo.sh` — Two new helpers, four call sites migrated.
- `tests/demo/test-demo-dataset.sh` — Two new guard suites (textual + behavioural).

**Change Summary:** All four non-privileged `demo.sh` callers moved from `GET /api/transactions/account/{id}` to `GET /api/transactions/my`, with PascalCase-tolerant filtering `(.accountId // .AccountId)`.

**Call Sites Migrated:**
1. `demo.sh:437` — `seed_transactions` idempotency pre-check
2. `demo.sh:671` — `resolve_account_refs` transaction count lookup
3. `demo.sh:684` — `build_unscored_fallback_refs` fallback subject selection
4. `demo.sh:1318` — verify pass (`cmd_show_summary`) per-account count (relabelled "owner's transaction(s)")

## Verification

- `bash -n scripts/demo/demo.sh` — passes syntax check.
- `tests/demo/test-demo-dataset.sh` — PASSED, 10 check groups, including three new: no account-scoped route calls, PascalCase filter guard, behavioural idempotency simulation with mixed-case fixtures.
- **Tamper test:** Reverting helper to bare `.accountId` fails both textual and behavioural guards. Reverted; suite green.
- **Idempotency offline simulation:** Five cases (reseed/camelCase, reseed/PascalCase, empty ledger, same description on different account, duplicate within one run) all decide correctly.

## Impact

**Brian's reseed unblocked.** The seeder now asks "what have I posted" through its own token's view instead of asking an account-scoped endpoint without entitlement on an empty ledger. No redeploy required.

## Deferred

No `task cloud:demo:reset`, no Azure write, no `kubectl` mutation, no commit. Live reseed is Brian's to run and is the remaining proof.
