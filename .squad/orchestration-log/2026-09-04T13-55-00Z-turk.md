### 2026-09-04T13:55:00Z — Banker Copilot backend policy engine design spike

| Field | Value |
|-------|-------|
| **Agent routed** | Turk (Backend Dev) |
| **Why chosen** | Backend policy engine design spike for approval ladder, declarative policy file, approval store schema, payload hashing, and audit integration |
| **Mode** | `background` |
| **Why this mode** | No hard dependency on other agents; can proceed independently |
| **Files authorized to read** | `docs/epics/banker-copilot.md`, `docs/design/banker-copilot-policy-engine.md`, existing Cosmos/audit patterns |
| **File(s) agent must produce** | `.squad/decisions/inbox/turk-policy-engine-design.md`, `docs/design/banker-copilot-policy-engine.md` |
| **Outcome** | Completed |

---

## Summary

Turk produced a backend policy engine design spike recommending Python 3.11/FastAPI (NOT .NET, conflicting with Danny's authority-service choice — requires Danny's ratification). Declarative `config/banker-copilot/policy.yaml` with 18 real mutating actions. Escalator monotonicity is structural (no downgrade/exempt/waive constructs). **Critical finding: single shared JWT audience across all services means no authorization boundaries — remediation: second `banking-copilot` audience; also requires splitting shared KSA for per-service mesh policy.** Approval store partition key `/requesterId` justified. Payload hashing with RFC 8785 + money-as-decimal strings + slot ordinality. Audit integration reuses existing `.NET payload`-envelope on `banking-events`. Lazy read-side check + sweeper + Cosmos TTL on terminal docs only. Identified 8 open questions for Danny; no code modified.
