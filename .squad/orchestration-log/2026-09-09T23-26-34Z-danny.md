# Orchestration Log — Danny (Lead/Architect)

**Date:** 2026-09-09T23:26:34Z
**Branch:** `332-beta`
**Session:** Scribe Commit Phase
**Work:** Seeded approval-rung nondeterminism ruling (REVISION 1 post-diagnostic)

## Summary

Issued REVISION 1 of the nondeterminism ruling, superseding §R1-§R8 with §R9-§R14 in light of Squad's diagnostic evidence confirming seed validity (state b-i). Original §R7 fix ruled defective — would produce a seeder that reliably dies ~21 of 23 runs. Ruled option (ii): move predicate into poll break condition; wait for BOTH `n_usable >= minScoredRequired` AND a flagged row on a seeded account clearing `flagged_transaction_dual_control_amount`.

Established that 20-minute TTL on `ttl_balance_adjust` is real defect: kills 8 of 10 seeded approvals mid-walkthrough. Ruled env override mechanism (verified in `PolicyLoader.cs:156-160`) is correct fix; no product default change.

Did NOT change product defaults. Product design stands as-is. Demo environment is caller's responsibility.

## Deliverables

- New: `docs/design/seeded-approval-rung-nondeterminism-ruling.md` (REVISION 1, §R1-§R14, full reasoning and implementation guards)
- Durable guard (recorded to agent history): any predicate used to SELECT a subject must be the predicate that TERMINATES the wait

## Outcome

**SUCCESS** — Defective fix ruled out, sound option chosen, durable principle for future work established.

## Notes

- The principle is generalisable: "A seeder must wait for the thing it will later require."
- The escape hatch (`--allow-unescalated`) exists because the fix legitimately depends on a model score. One low score must not leave Brian with no demo.
