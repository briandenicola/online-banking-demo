---
date: 2026-09-10T23:19:23Z
agent: Danny (Lead/Architect)
epic: 332 (Banker Copilot agentic harness)
branch: 332-beta
work_type: architecture ruling + verification
---

# Danny — Epic #332 merge readiness assessment + xfail prompt rulings

## Work Delivered

### Outcome
Two rulings delivered to inbox. Merge recommendation: do not merge tonight. Deploy + 3 browser runs (1h estimated), then merge.

### 1. Epic #332 Merge Readiness Assessment (Complete)
**File:** `.squad/decisions/inbox/danny-332-merge-readiness.md`

**Finding:** Work is done; proof is not. Neither the free-text planner nor the customer directory lookup has been deployed or observed in a browser.

**Verified independently (proven, not inferred):**
- Running copilot image lacks `intent_model.py`; does not appear on the path despite existing in source
- Directory endpoint returns 404 in the cluster; control probe (authentication on same service) returns 401
- Pods rolled 36 minutes before commits landed; every pod predates every change

**What is deployed and proven:**
- `actionId` path is real, exercised, and tested live
- Authority model is intact
- Signing gate end-to-end works; Brian personally signed a live approval at 4:16:45 PM

**Blocking merge:**
1. Deploy both services from `332-beta`
2. Run three free-text prompts in a real browser (read-only, reaching approval, refusing)
3. Confirm directory lookup resolves exact match and refuses ambiguous

**Non-blocking desirable:**
- Commit the 31-prompt acceptance suite (currently untracked)
- Label suite for what it proves: routing only, model does not run
- Add endpoint note on non-disclosure constraint to travel with code

**Risk analysis:**
- 31-prompt suite proves routing, not reasoning (model is injected, never invoked)
- Two demo prompts flagged as xfail; rulings follow separately
- One residual risk: non-disclosure rule lives one layer above the data (enforced in copilot resolver, exposed by user-service endpoint). Not a merge blocker; should be documented as ADR constraint on endpoint.

**Recommendation:** If no deploy/browser time available: cut free-text from demo, ship the `actionId` path only (proven, deployed, exercised live).

### 2. Two xfail Demo Prompts — Rulings (Complete)
**File:** `.squad/decisions/inbox/danny-two-xfail-prompts.md`

**Prompt A: "Compare dana's checking history against casey's"**

**Ruling:** BUILD IT. Traced approval hash preimage end-to-end: evidence is **not** in the preimage. Canonicalization risk is zero. Real hazards are elsewhere and all four are named:
- Evidence key collision in the accumulator (multi-subject runs overwrite, last-writer-wins)
- **🔴 CRITICAL BUG:** facts map uses first-writer-wins merge across subjects, carrying wrong customer into approval record
- Redaction scope (checked; safe)
- Citation key consistency (checked; safe)

**Additional ruling:** Per-invocation evidence keys within planner; authority-facing evidence always keyed by tool id (bare on first invocation, `#2` on second, etc.). `facts` map must carry only first-invocation tool results (new requirement). Three tests: bundle shape, `already_gathered` refusal still fires, facts carry no cross-subject merge.

**Prompt B: "Lower its risk score"**

**Ruling:** CUT THE UTTERANCE. Keep the capability (score override), which is already demonstrated by passing prompts. The model cannot deterministically resolve "offshore wire" to a transaction without model-driven semantic search, which violates §1.4 (model never chooses between candidates). Deterministic substring match is brittle and implementation-dependent.

**Retest plan:** Rewrite both xfails, do not delete. §A6 and §B4 specify the new assertions.

### 3. Subject Resolution and Risk-Score Override Authority (Complete)
**File:** `.squad/decisions/inbox/danny-free-text-planner-subject-resolution-and-score.md`

**Ruling 1 — Subject Resolution:** Add lookup tools. No banker-safe path exists today (verified independently). New authority constant `CustomerDirectoryLookup`. Lookup (not search): exact/prefix, 3-char min, 5-candidate cap, identity-plane projection only (id, username, display name, status; no email, PII, financial data). Terminal refusals on ambiguity and no-match. Refusal must never list candidates.

**Ruling 2 — Risk Score Override:** Model proposes `newScore` bounded; both L2 signers see it; it is hash-bound (payload value). No option C (human edit at sign time, without re-signing). Already solved by counter-proposal flow (new hash, fresh L2 chain).

**Ruling 3 — Constraints on model:** Ratio-floor threshold plus action rule raising to L3. Expressible in policy today.

**Ruling 4 — Payload domain constraints:** New gap (not in policy), separate from canonicalization rules. Define action `requiredFields` explicitly in policy or treat action `hashFields` as required.

**Five defects in Turk's design named:** Refusal xfail structure, evidence key boundary, facts-map cross-subject merge (🔴 critical), answer model prompt isolation, payload validation sequence.

---
