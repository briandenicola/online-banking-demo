# Orchestration Log: Danny — 2026-09-04T14:32:00Z

## Session: Banker Copilot epic — final ruling round + vocabulary reconciliation

**Task:** Arbitrate canonical vocabulary and implement shared-identifier contract test

**Duration:** Session turn

**Outcome:** SUCCESS

### Work Completed

- Conducted systematic cross-document naming audit. Found 17 identifier mismatches across epic, policy engine design, and UI design documents (Brian's grep found only 2).
- Arbitrated canonical vocabulary (8 primary decisions):
  - Entity noun: `approval` (replacing `proposal` as noun; `proposed` state and `propose` verb retained)
  - Supersede link field: `supersededByApprovalId`
  - Terminal reason value: `PAYLOAD_SUPERSEDED` (not `SUPERSEDED_BY_REPLAN`)
  - Entities clarified: `session` (banker's conversation) vs `run` (one intent→plan→tools→artifact cycle)
  - Action-type id format: `<domain>.<entity>.<verb>`
  - Endpoint prefixes: `/api/authority/*` and `/api/copilot/*`
  - Additional: `requesterId`, `copilot-approvals` PK, `apr_` event prefix, `expiresAt`/`terminalAt`, `requiredRung`, `requiredSigners`, `actionId`, `firedEscalators`, `Approval*` audit prefix, no `ApprovalCosigned` (folded into `ApprovalSigned` + `slotOrdinal`)
- Identified most dangerous finding (#4): 5 of 13 action-type ids disagreed between documents. Action-type ids are policy file primary keys; a mismatch is a silent policy miss (fallback becomes security behavior), not a crash.
- Identified three mismatches on second pass after believing sweep was complete: (a) Linus's `ApprovalState` union still carried deleted states `'expired'` and `'void'` — ratified decisions never propagated; (b) `requiredSignatureCount` had fourth spelling; (c) `voidedReason` free text used where closed `terminalReason` enum belongs.
- Generalized policyVersion contract test into shared-identifier contract test covering: terminalReason enum (4 values), supersede link, approval field names, 11 audit event names, trace frame kinds, 13 action-type ids, endpoint paths. CI grep gate added to scan three markdown files.
- Validated Turk's three findings: (a) supersede id as own field — agreed; (b) Cosmos enum enforcement not achievable — accepted, specified application-side enforcement with readers failing closed; (c) `executed` vs `execution.state` — confirmed, own document was wrong.

### Artifacts Produced

`docs/epics/banker-copilot.md`:
- §0.1 — new vocabulary section with canonical definitions
- §11.1, §11.2 — reference tables and vocabulary summary
- §5.3.1a — new shared-identifier contract test (generalized from policyVersion)
- Multiple sections updated: §5.1, §5.2, §5.3, §5.5, §5.7, §8.0, §9, §10

`docs/design/banker-copilot-policy-engine.md`:
- Vocabulary updates propagated across

`docs/design/banker-copilot-ui.md`:
- Vocabulary updates propagated across, including corrected `ApprovalState` union

Decision inbox: `danny-canonical-vocabulary.md`

### Key Insight

**Two people reasoning well, converging on the same idea, still produced a broken contract.** Shared vocabulary is not a product of everyone being careful; it is a product of something checking. Same principle as §5.1.1(c): a rule only holds if something enforces it. The shared-identifier contract test and CI grep gate enforce this going forward.

### Dependencies & Next Steps

- All three documents now aligned on canonical vocabulary
- Shared-identifier contract test ready for implementation in Phase 1
- #333 replay agent needs to use canonical terminology

---

**Related Issues:** #332

**Related Files:**
- docs/epics/banker-copilot.md
- docs/design/banker-copilot-policy-engine.md
- docs/design/banker-copilot-ui.md
- .squad/decisions/inbox/danny-canonical-vocabulary.md
