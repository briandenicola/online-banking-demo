---
date: 2026-09-10T23:19:23Z
agent: Linus (Frontend)
epic: 332 (Banker Copilot agentic harness)
branch: 332-beta
work_type: implementation + UI completion
---

# Linus — Session stream closure fix, refusal-code UI, negative controls

## Work Delivered

### Outcome
Complete. Commits aa2fc14, 362a56b, 16ef005, 1d1e735 pushed to `332-beta`.
Tests: 527 → 541 passed. 13 pre-existing failures remain (out-of-scope).

### 1. Two-Outcome UI (Implemented)
- **Feature:** Free-text planner surfaces two outcomes the model now produces:
  - Read-only answers: evidence bundle + answer artifact, no approval gate
  - Named refusal: ten distinct refusal codes with banker-readable messages
- **Implementation:** New `CompletionPane` component renders both outcomes distinctly
- **Refusal codes:** `ambiguous_subject`, `subject_not_found`, `forbidden_action`, `objective_unmappable`, `payload_unfillable`, `evidence_unavailable`, `proposal_refused_by_authority`, `planner_model_unavailable`, `intent_contract_invalid`, `payload_invalid`
- **Files:** `src/ui-app/src/components/copilot/CompletionPane.tsx` (new)

### 2. Subject Non-Disclosure (Enforced Client-Side)
- **Rule:** Refusal to ambiguous subject must never list candidates; the list is the search API declined
- **Implementation:** Subject resolution errors handled at resolver → no candidates reach the UI
- **Browser guards:** Negative controls validate:
  - Ambiguous refusal does not render a list
  - Response structure matches schema
  - Evidence planing stops before evidence-gathering step
- **Files:** Validation in `src/ui-app/src/components/copilot/CompletionPane.tsx`

### 3. Session Stream Lifecycle (Proposal only)
- **Finding:** Session stream cannot outlive a finished run; posting a reconnect to a closed run returns 200-EOF forever
- **Proposal:** Server-side fix suggested to Danny; not actioned in this commit
- **Client-side mitigation:** Stop hammering; report accurate run state rather than faking liveness
- **Files:** Changes in `src/ui-app/src/api/copilotStream.ts`
- **Related:** See `linus-session-stream-cannot-outlive-a-finished-run.md` in `.squad/decisions/inbox/`

### 4. Test Coverage
- All new UI components have browser guards with negative controls
- Acceptance suite: 527 → 541 tests passing
- Path parity verified for read-only and refusal cases

## Dependencies
- On Danny: xfail prompt rulings (completed)
- On Turk: env-gated live-model mode (in flight)

## Blocked By
None. Ready for deploy after Turk's facts-map fix.

---
