# Orchestration Log Entry

---

### 2026-05-14T15:28 — E2E Tests: Account opening resubmit workflow (#135 + #136)

| Field | Value |
|-------|-------|
| **Agent routed** | livingston (Tester/QA) |
| **Why chosen** | Playwright E2E coverage for state machine transitions, retry flow, terminal status rendering, contract validation |
| **Mode** | background |
| **Why this mode** | Parallel with Basher/Linus; tests can be written against contract (test.skip for backend-only scenarios); infrastructure already exists (auth fixture, sample docs) |
| **Files authorized to read** | `.squad/decisions/inbox/danny-135-136-plan.md` (test contract: scenarios, SSN triggers, expected codes); `.squad/decisions/inbox/copilot-directive-retry-cap.md` (retry cap=1 constraint); existing fixtures: `tests/e2e/fixtures/authFixture.ts`, `tests/fixtures/sample-documents/` |
| **File(s) agent must produce** | `tests/e2e/specs/core/account-opening-resubmit.spec.ts` (601 lines, 4 test suites, 7 scenarios); commits 464f7c5 + a15498f on `squad/135-136-account-opening-state-machine`; `.squad/decisions/inbox/livingston-135-136-tests.md` (test architecture and patterns) |
| **Outcome** | Completed — Comprehensive Playwright suite covering: Happy path (GET /status polling, customerExplanation verification); Failure+Retry (stageAttempts increment, 202 response); Retry Cap Exceeded (retry cap=1 enforced, 409 response); Validation (409 on non-failed, 404 on missing). 1 test runnable (happy path), 6 tests marked test.skip() pending backend/UI implementation |

---

## Summary

Livingston created comprehensive E2E test suite for #135 and #136:

**Test Scenarios:**

1. **Happy Path — Terminal State with Customer Explanation** (✅ Likely GREEN)
   - Submit application with valid form data
   - Upload photo_id and proof_of_address
   - Poll GET /api/account-opening/{id}/status every 2 seconds until terminal
   - Verify stages[] array present
   - Verify customerExplanation populated for terminal states
   - Verify polling stops after terminal

2. **Failure + Successful Retry** (⏸️ Skipped — pending backend /resubmit endpoint)
   - Submit with ssn: "9999" (triggers single failure)
   - Poll until status === "failed"
   - Verify lastError.retryable === true
   - POST /resubmit → expect 202 Accepted
   - Verify response includes resumedFromStage, attempt: 2
   - Poll until terminal state
   - Verify stageAttempts[failedStage] === 2

3. **Retry Cap Exceeded** (⏸️ Skipped — pending backend retry cap logic)
   - Submit with ssn: "8888" (always fails)
   - Wait for first failure → stageAttempts[stage] === 1
   - POST /resubmit → expect 202 (first retry accepted)
   - Wait for second failure → stageAttempts[stage] === 2
   - Verify lastError.retryable === false (cap enforced)
   - Second POST /resubmit → expect 409 Conflict with error: "retry_cap_exceeded"

4. **Validation Suite** (⏸️ Skipped — pending backend /resubmit endpoint)
   - POST /resubmit on non-failed status → 409 Conflict
   - POST /resubmit on non-existent application → 404 Not Found
   - POST /resubmit without auth → 401/403

**Test Infrastructure:**
- Location: tests/e2e/specs/core/account-opening-resubmit.spec.ts
- Dependencies: authFixture.ts, sample-documents/john-smith/, testHelpers.ts
- Environment: BASE_URL from CUSTOM_DOMAIN
- Health check: /api/account-opening/applications
- Serial mode for state machine workflows
- 2s polling cadence, 90-120s timeouts, graceful degradation

---

**Implementation dates:** 2026-05-14T15:28  
**Commits:** 464f7c5, a15498f  
**Branch:** squad/135-136-account-opening-state-machine  
**Status:** 1 test ready to run; 6 tests unblocked by Basher (backend /resubmit endpoint, SSN triggers) + Linus (UI implementation). Unblock path: implement backend → remove test.skip() → verify green
