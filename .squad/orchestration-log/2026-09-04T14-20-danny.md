# Orchestration Log: Danny — 2026-09-04T14:20:00Z

## Session: Banker Copilot epic — final ruling round + vocabulary reconciliation

**Task:** Arbitrate O9: terminal states and terminalReason (policy-voided approvals)

**Duration:** Session turn

**Outcome:** SUCCESS

### Work Completed

- Ratified Turk's choice: policy-voided approvals persist as `denied` carrying `terminalReason`. No first-class `voided` lifecycle state.
- Recorded reasoning for non-obviousness: fewer lifecycle states → fewer places to be wrong; `terminalReason` already carries the distinguishing semantics; keeps re-plan supersede and policy void the same shape.
- Established four conditions that make the ruling safe: mandatory `terminalReason`, closed enum (4 values), normative grouping rule in all consumers, full signature recording.
- Corrected three findings: (a) supersede id encoded in reason value — moved to own field; (b) audit event names — normalized to PascalCase to match repo patterns; (c) own §5.1 contradicted the ruling — corrected to immutable terminal record pattern.
- Identified composition requirement: #333 replay must receive `terminalReason` on terminal frame, else policy voids read as banker rejection.
- Flagged residual: `expired` state collapsing (same redundancy argument) not acted on unilaterally — deferred for Brian's ruling.

### Artifacts Produced

`docs/epics/banker-copilot.md`:
- §5.1.1 — new conditions section (mandatory, closed, grouped, recorded)
- §5.1, §5.2, §5.3, §5.3.2, §5.5, §5.7, §8.0, §9, §10 — updates to reflect immutable terminal pattern
- Worked example ($40k loan escalation)

Decision inbox: `danny-o9-terminal-reason.md`

### Dependencies & Next Steps

- Brian's ruling on residual (`expired` collapse) — recorded as defaulted to "no" for now, deferred to final ruling round
- Turk to apply ruling to policy engine design (§8.8 correspondence)
- Linus to handle UI distinctiveness (four terminal reasons must be visually different)

---

**Related Issues:** #332

**Related Files:**
- docs/epics/banker-copilot.md
- docs/design/banker-copilot-policy-engine.md
- .squad/decisions/inbox/danny-o9-terminal-reason.md
