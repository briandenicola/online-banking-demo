# Orchestration Log — Linus — 2026-09-09

**Agent:** Linus (Frontend) · **Status:** Factor-divergence indicator merged, wording flagged for Danny, tests all green.

## Work Delivered

**Files Modified:** `src/ui-app/` only (no backend, no redeploy).
- `approvalPolicy.ts` — `Disagreement` gains `factorComparison` field ('compared' | 'primary_stated_no_factors' | 'no_factors').
- `supervisorFactors.ts` — Visibility condition and copy: *"Factor comparison unavailable — the primary agent stated no key factors."*
- `ApprovalCard.tsx` — Renders label in supervisor column (where `← DIVERGENT` flags would be).
- `__tests__/factorRow.test.tsx` — Five tests including present-when-primary-stated-none and absent-when-both-stated-factors.

## Implementation Detail

Informational (`info.main`), not error. Renders inline, not in tooltip. Principle: "We could not check" and "we checked and it was fine" must not look the same on a card a supervisor signs from.

## Verification

- `npx react-scripts test --watchAll=false --testPathPattern "copilot|demoFixture|authorityWire"` — 228 passed, 15 suites.
- Tamper: `{false && …}` on render condition → 2 failures (missing testid), both new. Reverted; green.
- `tsc --noEmit` — clean (two pre-existing tsconfig deprecation warnings, unrelated).
- 13 pre-existing `account-opening` UI failures untouched.

## Flagged for Danny's Review

**Wording deviation:** Shipped *"stated no key factors"* instead of Danny's *"does not emit key factors"* because primary now emits factors on happy path. Run-scoped wording is correct in both worlds. §F5 says "something of the form" — latitude sufficient for tense change? **Needs confirmation or overrule.**

**Second-layer silence:** `factorComparison === 'compared'` (both sides stated factors) but neither side classifies factors. Divergence guard only fires where both set `concern` to explicit booleans (never). Gap survives one layer deeper. Linus flagged rather than widening label (would fire on every card). **Danny's to rule: cover now, or defer with §F7?**

## Deployment

Frontend only. Needs `ui-app` image rebuild to reach cluster. **Rides the next image. No immediate redeploy.**
