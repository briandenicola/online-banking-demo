# Orchestration Log — Rusty (Platform)

**Date:** 2026-09-09T23:37:00Z
**Agent:** Rusty (Platform)
**Task:** Applied 8-hour approval TTL override to demo environment via `deploy/kustomize/base/configmap.yaml`

## Outcome: SUCCESS

### Work Performed

1. **ConfigMap patch applied** — all 9 approval TTLs set to `28800` (8 hours) via environment variables on `banking-demo-config`.
2. **Verification from running cluster** — `GET /api/authority/policy` confirmed all 9 TTLs report `28800` with `overriddenByEnv: true`.
3. **File validation** — `git diff config/authority-policy.yaml` is empty; shipped defaults remain unchanged.
4. **Pod status** — `authority-service` 2/2 Running, 0 restarts. Image digest identical before/after; no rebuild.

### Corrections to Brief

- The TTL set is **9, not 8** — `ttl_loan_decision` was missing from the provided list.
- The default TTL's env key is **`POLICY_APPROVAL_TTL_SECONDS`**, not `POLICY_TTL_*`.

### Ruling Citation Correction

Brief cited **§R11** (`.id` lookup guard). The TTL override ruling is **§R12** (`docs/design/seeded-approval-rung-nondeterminism-ruling.md`).

---
