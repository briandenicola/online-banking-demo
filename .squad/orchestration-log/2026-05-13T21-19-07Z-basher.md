# Basher Orchestration Log — 2026-05-13T21:19:07Z

## SPAWN MANIFEST

**Agent:** Basher (background, claude-sonnet-4.5)  
**Task:** Revert account-opening-service from Entra Agent ID sidecar back to DefaultAzureCredential / workload identity (issue #134)  
**Status:** ✅ Implemented (awaiting Brian deploy)

## Changes Summary

**Modified Files:** 4

1. **`src/account-opening-service/app/worker.py`**
   - Removed `from .sidecar_credential import SidecarTokenCredential` import
   - Deleted `AGENT_ID_SIDECAR_URL` / `AGENT_ID_AGENT_IDENTITY` branch
   - `foundry_credential` now unconditionally uses `DefaultAzureCredential`

2. **`src/account-opening-service/app/sidecar_credential.py`**
   - Added deprecation comment: `# DEPRECATED 2026-05-13 (issue #134): Reverted to DefaultAzureCredential / workload identity. Kept for potential future re-enable.`
   - File retained (not deleted)

3. **`src/account-opening-service/README.md`**
   - Removed `AGENT_ID_SIDECAR_URL` and `AGENT_ID_AGENT_IDENTITY` environment variable entries

4. **`deploy/kustomize/base/account-opening-service.yaml`**
   - Removed `entra-agent-id` sidecar container
   - Removed `sidecar-keys` projected volume and volumeMount
   - Removed `AGENT_ID_SIDECAR_URL=http://localhost:8080` env var
   - Pod now matches `ai-service.yaml` pattern (init + main + istio)

## Diff Statistics

```
deploy/kustomize/base/account-opening-service.yaml    | 30 -----------------------
src/account-opening-service/README.md                 |  2 --
src/account-opening-service/app/sidecar_credential.py |  1 +
src/account-opening-service/app/worker.py             | 20 ++++-----------
```

## Verification (by inspection)

✅ `worker.py` parses successfully  
✅ No remaining references to `SidecarTokenCredential`, `AGENT_ID_SIDECAR_URL`, or `entra-agent-id`  
✅ Pod spec now: init `provision-agents` + main `account-opening-worker` + istio-proxy  
✅ Mirrors `ai-service.yaml` workload-identity pattern  

## Reference Pattern

**Account-opening-service workload identity now mirrors ai-service.yaml:**
- Uses `banking-workload-identity` ServiceAccount with Entra federated credentials
- `DefaultAzureCredential` in Python code handles token acquisition automatically
- No sidecar container needed

## Out of Scope

- `deploy/kustomize/base/configmap.yaml` still has `AGENT_ID_AGENT_IDENTITY` placeholder (harmless, can be cleaned up in future config pass)
- `sidecar_credential.py` retained intentionally per Brian's instruction

## Deployment Notes

**Brian handles:**
1. Code review of 4 files
2. `docker build` and image push
3. `kubectl apply -k deploy/kustomize/base/` to deploy
4. Monitor account-opening-worker logs for clean Foundry agent initialization

**No RBAC changes required** — workload identity already configured with appropriate scopes.

---

**Awaiting:** Brian approval and deployment
