# Orchestration Log — Rusty (Platform/Infra)

**Date:** 2026-09-09T23:26:34Z
**Branch:** `332-beta`
**Session:** Scribe Commit Phase
**Work:** Seeder qualifying-subject poll predicate implementation (implements Danny §R10 option ii)

## Summary

Implemented Danny §R10 option (ii) in `scripts/demo/demo.sh`: the scoring poll now waits for BOTH `n_usable >= minScoredRequired` AND a flagged row on a seeded account clearing the live `flagged_transaction_dual_control_amount`. Deleted the silent `flagged_pool="$pool"` fallback in favour of two dies with distinct failure modes. Added `--allow-unescalated` opt-out following existing `--allow-unscored` precedent.

Statically and behaviourally verified: `bash -n` clean, threshold resolved live not hardcoded, guard suite passed with 10 check groups. No Azure writes performed.

## Deliverables

- Modified: `scripts/demo/demo.sh` (new `qualifying_flagged_pool()`, break condition, selection logic, die messages, opt-out)
- Modified: `config/demo-dataset.json` (escalator declaration, scoring comment)
- Modified: `tests/demo/test-demo-dataset.sh` (static guard widened to include action-local rules)

## Outcome

**SUCCESS** — Implementation verified static and dynamic. Not yet proven by live reseed (Brian's next action). No writes to Azure, Foundry, or cluster.

## Notes

- Expect ~2-3 minutes for scoring stage vs ~45s today due to waiting for class of transaction.
- The same `qualifying_flagged_pool()` function is called from break condition AND selection — that is the whole point. Prevents drift.
- Risk: if model scores large wires below 0.7 tomorrow, seeder dies. `--allow-unescalated` is one-keystroke to degrade to L1 instead.
