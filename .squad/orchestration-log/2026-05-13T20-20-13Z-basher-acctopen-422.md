# Orchestration Log: basher-acctopen-422 (2026-05-13T20:20:13Z)

**Agent:** Basher (Backend Dev)  
**Mode:** Background (sonnet 4.5)  
**Status:** Complete  
**Task:** Diagnosed multipart contract mismatch (`files` vs `file`) + DocumentUpload missing resolveApiError()  

## Deliverables

✅ **basher-acctopen-422.md** — Account opening document upload regression analysis
- Symptom: 422 Unprocessable Content + React error #31 (white screen) on document upload
- Root cause (primary): Client/server contract mismatch (`files[]` vs `file`)
- Root cause (secondary): DocumentUpload.tsx doesn't use `resolveApiError()` utility (leaves non-string in error state)
- UI-only fixes required (no backend changes)

## Technical Details

### 422 Error
- **Client sends:** `files[]` (plural) in FormData
- **Backend expects:** `file` (singular) in `upload_document()` endpoint
- **FastAPI validation fails:** Pydantic error returned with array of validation errors

### React #31 Error
- **Cause:** DocumentUpload.tsx extracts `detail` without type checking
- **Result:** Non-string (array/object) passed to `setError()` → JSX renders object → React crashes
- **Solution exists:** Commit #127 created `resolveApiError()` utility; ApplicationForm already uses it

## Code References

- **Primary:** `src/ui-app/src/api/accountOpening.ts:125` — change `'files'` → `'file'` OR implement sequential upload
- **Secondary:** `src/ui-app/src/components/account-opening/DocumentUpload.tsx:348-374` — import and use `resolveApiError()`

## Recommended Fix (Option A)

1. Change FormData field name to singular `'file'` 
2. Import `resolveApiError` from `../../api/errors`
3. Replace error extraction logic with single call to `resolveApiError(err, fallback)`

## Files to Merge

- ✅ `.squad/decisions/inbox/basher-acctopen-422.md` → `decisions.md` (diagnosis + fix options)

## Status

Ready for Basher to implement UI-only fix + update tests.
