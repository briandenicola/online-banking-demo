# Orchestration Log — Squad (Coordinator)

**Date:** 2026-09-09T21:42:28Z
**Branch:** `332-beta`
**Session:** Scribe Pass
**Work:** Scoring poll interval tuning

## Summary

Raised `scoring.pollSeconds` in `config/demo-dataset.json` from 90 to 300 and added explanatory `_scoringComment`. Reason: Azure AI Foundry rate-limits every ai-service scoring call with HTTP 429; the service retries after ~3s, so scoring costs ~5–6s per transaction. 23 seeded transactions cannot complete inside a 90s window. This is throttling, not a fault.

## Deliverables

- Modified: `config/demo-dataset.json` (2 changes: `scoring.pollSeconds` 90 → 300, and new `_scoringComment` field with explanation)

## Outcome

**SUCCESS** — Reseed completed end to end, `Seed complete`. Azure Foundry HTTP 429 behavior confirmed to be rate-limiting, not a service fault. Polling window now accommodates all 23 transactions under production throttling.

## Notes

- Each transaction's ai-service scoring call suffers HTTP 429 and retries after ~3s. With 23 transactions, 90s is insufficient. 300s (5 minutes) provides headroom.
- Coordination required on Brian's explicit approval to modify the dataset.
