# Orchestration Log: Turk — 2026-09-04T14:45:00Z

## Session: Banker Copilot epic — final ruling round + vocabulary reconciliation

**Task:** Apply four final rulings to policy engine design (status, execution, denial reason, step-up auth)

**Duration:** Session turn

**Outcome:** SUCCESS

### Work Completed

- Applied lifecycle collapse (no `expired` state, §5.3.1): `proposed → pending → signed → executed`, `denied` single terminal state with four-value `terminalReason` enum. Sweeper mechanism unchanged, now writes `denied` + `TTL_EXPIRED`. Added explicit call-out that expiry still means denied, never auto-approved (visibility loss when state collapsed).
- Noted that `status = "denied"` now much weaker signal; any bare `status='denied'` filter is almost certainly a bug.
- Applied Q2 ruling (§8.5.1): `payloadHash` permanent on every approval representation (list, detail, sign response, SSE events). Added non-removable marking. Computed `payloadHashShort` server-side so UI never truncates security value.
- Applied Q3 ruling (§8.7.1): Denial reason required, 6-layer validation in `authority-service`. Rules: NFC-normalize, trim, collapse internal whitespace for measurement only, then measure in grapheme clusters (not bytes/UTF-16) so non-Latin reasons don't need triple the substance. Repeated-unit check kills degenerate input. All six config keys with env overrides; defaults documented, not literals. Acknowledged that rules stop lazy input, not determined garbage.
- Applied Q4 ruling (§8.6.1): **NO** — banker's own second signature never suffices at L2. MFA proves "who", SoD proves "how many"; neither substitutes for the other. Enforced structurally: `mustDifferFrom` built by evaluator and no policy verb can empty it.
- Rejected coordinator's instruction to "enforce enum at persistence layer" as unachievable in Cosmos (no CHECK constraints, no server-side schema). Specified four-layer enforcement: (a) C# enum with throwing converter, (b) single-writer repository type with architecture test, (c) guard query alerting on unknown values without self-healing, (d) readers fail closed on unrecognized reason.
- Corrected own earlier document: `execution_failed` is not a terminal lifecycle status. Failed execution stays `status = signed` / `execution.state = failed`; retry needs no new human signature but DOES re-enter policy re-evaluation gate (risk 4 implication).
- Identified Cosmos index change: "Show me what expired" was `status='expired'`, single-field index. Now two-predicate filter plus sort. Composite index `(status, terminalReason, terminalAt)` required; without it query degrades to cross-partition scan. `terminalAt` must now be reliably populated on every terminal transition (was previously nullable-and-ignored).
- Retained `ApprovalExpired` event name even though state collapsed (stream append-only). Event says what happened; `terminalReason` says what it means.
- Added config keys: `DENIAL_REASON_MIN_LENGTH`, `DENIAL_REASON_MAX_LENGTH`, `DENIAL_REASON_MIN_DISTINCT_CHARS`, `DENIAL_REASON_MAX_REPEAT_UNIT`, `DENIAL_REASON_MIN_LETTERS` to both `deploy/kustomize/base/configmap.yaml` and `docker-compose.yml`.

### Artifacts Produced

`docs/design/banker-copilot-policy-engine.md` (2035 lines):
- §5.3.1, §5.4 — lifecycle collapse with mechanisms and call-outs
- §8.5.1 — payloadHash permanence
- §8.6.1 — Q4 ruling with structural enforcement
- §8.7.1 — Q3 six-layer validation rules
- §8.8 — failed execution doesn't move status (correspondence with §5.1)

Three important corrections flagged:
1. Supersede reason not a constant (moved to field), policy version not interpolated
2. Enum enforcement is application-side not database-side, readers fail closed
3. Execution failure doesn't move status, retry re-enters policy gate

Decision inbox: `turk-final-rulings-implementation.md`

UI requirement note for Linus: all four terminal reasons now share `status = "denied"`, so branching on `status` alone is a bug; must be visually distinct. Plus permanent `payloadHash` display.

### Technical Decisions

- **Guard Query:** Alerts on unrecognized `terminalReason` values without self-healing (evidence preservation)
- **Reader Behavior:** Unrecognized reason → treat as `"denied and not executable"` (fail closed)
- **Index Strategy:** Composite on (status, terminalReason, terminalAt) to avoid cross-partition scans
- **Retry Flow:** No new signature needed but full policy re-evaluation required

### Dependencies & Next Steps

- Config key implementation in both manifests
- UI implementation: Q2 payloadHash on read model, Q3/Q4 implications on signature/denial flows
- Architecture test for single-writer repository pattern
- Guard query deployment and alerting setup

---

**Related Issues:** #332

**Related Files:**
- docs/design/banker-copilot-policy-engine.md
- deploy/kustomize/base/configmap.yaml
- docker-compose.yml
- .squad/decisions/inbox/turk-final-rulings-implementation.md
