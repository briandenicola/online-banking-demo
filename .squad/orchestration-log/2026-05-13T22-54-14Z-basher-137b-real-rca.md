# Orchestration Log: basher-137b (Real RCA for Eval-403)

**Timestamp:** 2026-05-13T22:54:14Z  
**Agent:** basher-137b  
**Model:** claude-opus-4.7  
**Mode:** background  
**Status:** COMPLETED  

## Summary

Deep-dive investigation that identified the true root cause of issue #137 (Eval-403). The 403 was **not** an RBAC or SDK drift issue — it was an incomplete eval payload with missing assistant turn in the conversation.

## Work Done

- Bisected to commit 39dfdbe (P2 Wave 1 refactor #114) which dropped the per-transaction `eval_agent.run()` call
- Traced the missing assistant `Message` through the eval payload construction
- Identified that Foundry SDK derives `response_text` from assistant Messages; with no assistant turn, `response_text == ""` and raisvc rejects with misleading 403
- Found residual dead variable `eval_agent` in `/api/admin/evaluate` endpoint
- Located secondary cosmetic regression in anomaly_service.py (commit 243457f) where warm-up token scope was silently reverted
- Restored assistant turn in eval payload construction (commit 46d712a)
- Reverted cosmetic scope regression in anomaly_service.py
- Posted full RCA comment on issue #137

## Outcome

✅ **SUCCESS.** Real root cause identified and fixed. The pinning from basher-137 was necessary (for other drift issues) but not sufficient — the structural eval payload bug was the blocker.

## Files Modified

- `src/ai-service/app/routes/api.py` (restored eval_agent.run() + assistant Message)
- `src/ai-service/app/services/anomaly_service.py` (reverted warm-up scope, removed cosmetic regression)

## Decision Captured

Decision stored in `.squad/decisions/inbox/basher-137-eval-403-real-rca.md` (full diagnostic ladder for raisvc 403s; skill captured in `.squad/skills/foundry-eval-debugging/SKILL.md`).

## Follow-ups Recommended

1. **Integration test:** Mock evals.create/runs.create and assert non-empty `response` per item
2. **Audit #114 refactor:** Sweep other services for similar dead-variable regressions
3. **Microsoft feedback:** File that raisvc should distinguish missing-assistant-turn from RBAC denial
