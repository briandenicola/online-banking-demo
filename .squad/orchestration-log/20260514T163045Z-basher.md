# Orchestration: Basher — Fix deploy task to sed-substitute ACR name

**Date:** 2026-05-14T163045Z  
**Agent:** Basher  
**Task:** Fix deploy task to sed-substitute ACR name in kustomization.yaml from TF output

## Outcome

**✅ Persistent fix landed** — `_kustomization:update` task added to `tasks/Taskfile.cloud.yml` + missing `account-opening-service` line added to `_images:update`.

Wrote decision:  
- `.squad/decisions/inbox/basher-kustomize-acr-template.md` — full decision rationale
- `.squad/skills/deploy-task-acr-templating/SKILL.md` — reusable skill for future env-specific manifest templating

**Working tree stayed clean** — File restored via `git checkout` post-apply, matching existing `_configmap:update` / `_secretproviderclass:update` pattern.

## Side Work (Wasted — Wrong Workspace Context)

Basher also:
- Hardcoded 11 `newName:` entries to `poeticanemone22804acr` (WRONG ENV — workspace was `canadacentral`, not `swedencentral`)
- Manually created AcrPull role assignment in the OLD env via `az CLI`
- Flagged as "TF drift" — but was actually workspace context noise, now harmless

**Why harmless:** The new sed templating will rewrite kustomization.yaml on next deploy, overwriting the hardcoded values.

## Root Cause (Coordinator discovery)

Both the `ImagePullBackOff` and the workload-identity 401 (AADSTS70025 on client 51592ddd, old MI) were caused by:

**TF working tree on wrong workspace.** Basher was working in `canadacentral` workspace (`poetic-anemone-22804`) instead of `swedencentral` (`funky-elephant-11797`). Every TF output the deploy task reads was returning OLD env values.

Fix: `terraform -chdir=./infra/cloud workspace select swedencentral` before `task cloud:deploy`.

**This was NOT a code bug** — it was a workspace context bug.

## Artifacts

- Taskfile change: `tasks/Taskfile.cloud.yml` (_kustomization:update + _images:update)
- Skill documentation: `.squad/skills/deploy-task-acr-templating/SKILL.md`

## Follow-Up

New user directive (captured in inbox): **Agents must NEVER run `task cloud:deploy` themselves.** Brian manages all deploys. Agents may edit deploy task files and propose redeploys, but invocation is Brian's.
