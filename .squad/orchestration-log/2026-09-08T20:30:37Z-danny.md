# Orchestration Log Entry

> One file per agent spawn. Saved to `.squad/orchestration-log/{timestamp}-{agent-name}.md`

---

### 2026-09-08 — Rule on Gate B (the evidence contract seam)

| Field | Value |
|-------|-------|
| **Agent routed** | Danny (Lead/Architect) |
| **Why chosen** | Gate B blocking the entire L2 co-signature feature requires architectural decision on how evidence is described, validated, and stored. Danny holds authority over policy semantics and cross-service contracts. |
| **Mode** | `sync` (escalation) |
| **Why this mode** | Architectural ruling requires human decision; Turk and Livingston are blocked. No implementation occurs until the ruling is accepted. |
| **Files authorized to read** | `livingston-supervisor-agreement-measurement.md`, `turk-phase3-supervisor-boundary-and-envelope.md`, `.squad/identity/now.md`, `config/authority-policy.yaml`, `config/copilot-tools.yaml`, `src/authority-service/Policy/PolicyEvaluator.cs`, `src/banker-copilot-service/app/tools/manifest.py`, `src/banker-copilot-service/app/planner/loop.py`, `scripts/demo/evidence-contract.py` |
| **File(s) agent must produce** | `danny-evidence-contract-ruling.md` with R0–R9 sections covering: the ruling (adopt declared projection), three amendments, rejection of alternatives, grammar constraints, the `list_login_audits` exception, `get_user.status` policy correction, seam test location in C#, and the cross-cutting pattern finding |
| **Outcome** | Completed — ruling written to `.squad/decisions/inbox/danny-evidence-contract-ruling.md` (~21KB). Upheld Turk's declared projection (`evidenceProjection` in `config/copilot-tools.yaml`) with three amendments: closed grammar (4 verbs, no literals, lossless), `list_login_audits` stays blocked (no projection), and `get_user.status` is policy-side correction not rename. Found NEW defect (§R5) that would have put false statement in audit record. Rejected relax-EvidenceComplete (would produce tautology + lie), rejected tool shape changes (breaks public API), accepted adapter pattern. Nothing implemented. |

---

## Notes

Danny's ruling was required to unlock implementation. The new defect in `list_login_audits` (§R5) was caught by architecture review and quarantined before it could ship. The ruling includes deferred-before-`main` items (§R3 provenance envelope, §R4 evidence↔payload identity check) that are outside this session's scope.
