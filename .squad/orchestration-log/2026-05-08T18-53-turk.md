### 2026-05-08T13:53:00Z — KeyVault CSI Driver Secrets Migration

| Field | Value |
|-------|-------|
| **Agent routed** | Turk (Backend Dev) |
| **Why chosen** | Feature involves secrets management, cross-service configuration consistency, Python/Go service integration — Turk's focus on backend infra |
| **Mode** | `background` |
| **Why this mode** | No hard data dependencies; runs in parallel with Basher's TLS work |
| **Files authorized to read** | `.squad/agents/turk/charter.md`, `.squad/agents/turk/history.md`, `Taskfile.cloud.yml`, `deploy/kustomize/base/`, `infra/cloud/`, `docker-compose.yml` |
| **File(s) agent must produce** | `infra/cloud/keyvault-secrets.tf`, `deploy/kustomize/base/secret-provider-class.yaml`, all 8 service deployments (CSI volume mounts), `Taskfile.cloud.yml` (simplified _secrets:create), `infra/cloud/outputs.tf` (added key_vault_name) |
| **Outcome** | Completed |

---

**Artifacts Created:**
- `infra/cloud/keyvault-secrets.tf` — Terraform for 4 KeyVault secrets + CSI RBAC (kubelet identity role assignment)
- `deploy/kustomize/base/secret-provider-class.yaml` — SecretProviderClass maps KV secrets to K8s Secret `banking-secrets`
- All 8 service deployments — Added CSI volume + volumeMount (no env var changes needed)
- `infra/cloud/outputs.tf` — Added `key_vault_name` output for Taskfile consumption
- `Taskfile.cloud.yml` — Simplified `_secrets:create` task, updated deploy pipeline

**Decision Log:**
- Decision: Migrate Secrets to Azure KeyVault with CSI Driver (turk-keyvault-csi.md)

**Pattern Established:**
- JWT key is stable via Terraform `random_password` (not regenerated per deploy)
- CSI driver uses kubelet managed identity (not workload identity) — separate RBAC config required
- Placeholder pattern: REPLACE_WITH_KEYVAULT_NAME, REPLACE_WITH_TENANT_ID, REPLACE_WITH_AZURE_CLIENT_ID
- Observability namespace keeps simple `kubectl create secret` (one-off pattern acceptable for single secret)
