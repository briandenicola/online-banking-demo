### 2026-05-08T13:53:00Z — TLS Cert-Manager + Istio Gateway HTTPS

| Field | Value |
|-------|-------|
| **Agent routed** | Basher (Backend Dev) |
| **Why chosen** | Feature involves infrastructure security (TLS termination), Azure cert-manager integration, Istio gateway configuration — Basher's domain |
| **Mode** | `background` |
| **Why this mode** | No hard data dependencies; other agents can work in parallel |
| **Files authorized to read** | `.squad/agents/basher/charter.md`, `.squad/agents/basher/history.md`, `cluster-config/`, `Taskfile.cloud.yml`, `.env.example` |
| **File(s) agent must produce** | `cluster-config/cert-manager/clusterissuer.yaml`, `cluster-config/cert-manager/certificate.yaml`, `cluster-config/istio/gateway/istio-ingress-gateway.yaml` (updated), `Taskfile.cloud.yml` (added tls:* tasks), `.env.example` (added CUSTOM_DOMAIN) |
| **Outcome** | Completed |

---

**Artifacts Created:**
- `cluster-config/cert-manager/clusterissuer.yaml` — Let's Encrypt HTTP-01 ClusterIssuer with `class: istio`
- `cluster-config/cert-manager/certificate.yaml` — Certificate resource template using `${CUSTOM_DOMAIN}`
- `cluster-config/istio/gateway/istio-ingress-gateway.yaml` — Updated with HTTPS server (port 443, TLS mode SIMPLE) + HTTP redirect
- `Taskfile.cloud.yml` — Added `tls:install-cert-manager`, `tls:setup`, `tls:status` tasks
- `.env.example` — Added `CUSTOM_DOMAIN` variable

**Decision Log:**
- Decision: TLS Termination via cert-manager on Istio Ingress (basher-tls-cert-manager.md)

**Pattern Established:**
- For managed AKS Istio, TLS certs reside in `aks-istio-ingress` namespace
- `CUSTOM_DOMAIN` env var drives certificate domain via `envsubst`
- HTTP-01 solver works with `class: istio` without DNS provider
- Operational tooling (Helm install) stays in Taskfile, not Terraform
