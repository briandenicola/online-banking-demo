# Orchestration Log — Danny (Lead/Architect)
**Session:** 2026-05-05 16:30  
**Role:** Architecture & Project Constitution Review  
**Status:** ✅ Completed

## Summary
Comprehensive architecture and infrastructure audit of online-banking-demo monorepo. Scope included project structure, microservice architecture, CI/CD pipelines, configuration/environment handling, IaC quality, security patterns, and documentation.

## Key Findings

### 🟢 Strengths
- Clean monorepo layout with clear service boundaries
- Event-driven architecture via Azure Event Hub with typed contracts
- Infrastructure split (local dev vs cloud) enables zero-dependency developer experience
- Workload Identity federation properly configured for cloud deployment
- Taskfile automation well-organized across local and cloud concerns

### 🔴 Critical Issues (3)
1. **CI Dockerfile context mismatch** — CI uses per-service context but .NET builds expect repo root (contains COPY src/shared/)
2. **Duplicate Terraform resource** — azurerm_user_assigned_identity.openai_managed_identity defined twice in cloud/main.tf
3. **Missing federated identity attribute** — azurerm_federated_identity_credential missing user_assigned_identity_id

### 🟡 Warnings (8)
- JWT secret hardcoded in docker-compose.yml (security anti-pattern)
- No `.env.example` for developer onboarding
- Redis declared but unused (vestigial infrastructure)
- No test execution in CI pipeline (only dotnet build on Contracts)
- No image tagging strategy (all `:latest`)
- No CORS configuration in nginx gateway
- No health check endpoints on individual services
- No API versioning or rate limiting

## Impact on Other Teams
- **Basher**: CI context mismatch will fail .NET builds; verify partition key setup relies on correct Cosmos partitioning
- **Linus**: No CORS means frontend cross-origin requests blocked; API gateway lacks auth middleware
- **Livingston**: CI doesn't execute tests despite "test" job existing; no integration test framework in place

## Recommendations Priority
1. Fix CI Dockerfile contexts (P0 — blocks CD)
2. Fix Terraform syntax errors (P0 — blocks infrastructure)
3. Configure CORS and gateway auth (P1 — security)
4. Remove hardcoded JWT key (P1 — security)
5. Add .env.example (P2 — developer experience)
