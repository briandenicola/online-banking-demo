# Orchestration Log Entry

> One file per agent spawn. Saved to `.squad/orchestration-log/{timestamp}-{agent-name}.md`

---

### 2026-05-13T02:47:00Z — Coordinator: Deployment Configuration & Container Fixes

| Field | Value |
|-------|-------|
| **Agent routed** | Coordinator (User/Brian via Copilot) |
| **Why chosen** | Cross-cutting infrastructure and deployment concerns; needed to unblock builds and address sidecar/dependency issues |
| **Mode** | `sync` |
| **Why this mode** | User involvement in deployment verification and decision-making |
| **Files authorized to read** | `.dockerignore`, `docker-compose.yml`, all service Dockerfiles, account-opening-service requirements.txt, deployment configs |
| **File(s) agent must produce** | Updated `.dockerignore`, relaxed azure-ai-inference constraint, added python-multipart + aiohttp deps, fixed entra-agent-id sidecar port |
| **Outcome** | Completed — all containers built via ACR, deployed to AKS (banking-demo namespace), verified healthy. Pushed to origin/main. All P1 issues closed on GitHub. |

---

## Changes Summary

- **Build artifacts exclusion:** Added `**/obj.old/` to .dockerignore (excluded stale root-owned build artifacts from layers)
- **Dependency relaxation:** Relaxed `azure-ai-inference` to `>=1.0.0b9,<2.0.0` (no stable release exists; beta required)
- **Account-opening-service:** Added `python-multipart` and `aiohttp` dependencies (multipart form handling, async http client)
- **Entra agent-id sidecar:** Fixed port from 5000 → 8080 (default listening port for sidecar container)
- **Deployment lesson:** Always use `task cloud:deploy` — never `kubectl apply -k` directly. Taskfile handles placeholder substitution.
