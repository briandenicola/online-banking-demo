# Orchestration Log Entry

### 2026-05-19T14:03:03Z — Fix useradd build failures across 5 Python Dockerfiles

| Field | Value |
|-------|-------|
| **Agent routed** | Turk (Backend Dev) |
| **Why chosen** | Dockerfiles belong to backend services. Turk specializes in infrastructure and container patterns. |
| **Mode** | background |
| **Why this mode** | No hard data dependencies; Turk can operate autonomously to fix Dockerfiles, update skill documentation, and validate via decision records. |
| **Files authorized to read** | src/chatbot-service/Dockerfile, src/budget-service/Dockerfile, src/ai-service/Dockerfile, src/account-opening-service/Dockerfile, src/ai-service/Dockerfile.eval-debug, .squad/skills/mcr-base-image-migration/SKILL.md |
| **File(s) agent must produce** | 5 modified Dockerfiles (all in `/src/*/Dockerfile{,.eval-debug}`), updated SKILL.md with Common Gotchas section, .squad/decisions/inbox/turk-mcr-base-image-migration.md |
| **Outcome** | ✅ Completed — Dockerfiles fixed. Replaced `RUN useradd ... + USER appuser` with `USER 1001` (or `USER 1000` for eval-debug to match its k8s runAsUser). Azure Linux base/python images don't ship shadow-utils; numeric UID is the recommended minimal-image pattern. Skill documentation updated with Common Gotchas section. |

---

## Execution Summary

**Execution Date:** 2026-05-19T14:03:03Z  
**Status:** SUCCESS  

### Work Completed

1. **Root Cause Analysis**
   - Azure Linux base/python:3.12 image does NOT include shadow-utils package
   - `useradd` command fails with "command not found" during Docker build
   - Turk identified numeric UID approach as recommended pattern per Azure best practices

2. **Dockerfiles Fixed** (5 files)
   - `src/chatbot-service/Dockerfile` → `USER 1001`
   - `src/budget-service/Dockerfile` → `USER 1001`
   - `src/ai-service/Dockerfile` → `USER 1001`
   - `src/account-opening-service/Dockerfile` → `USER 1001`
   - `src/ai-service/Dockerfile.eval-debug` → `USER 1000` (matches k8s runAsUser: 1000)

3. **Documentation Updates**
   - `.squad/skills/mcr-base-image-migration/SKILL.md` — Added "Common Gotchas" section (lines 291-325) with:
     - Clear explanation: Azure Linux base images lack shadow-utils
     - Recommended solution: Use numeric UIDs (`USER 1001` or `USER 1000`)
     - Why numeric UIDs are better: No dependencies, portable, Kubernetes-safe, smaller attack surface
     - When to use specific UIDs (with examples for k8s runAsUser)
     - File ownership guidance

4. **Decision Record**
   - Created `.squad/decisions/inbox/turk-mcr-base-image-migration.md` documenting:
     - Problem: Docker Hub rate limit during MCR migration
     - Decision: Migrate all base images to MCR (no rate limits)
     - Image mapping table with all 9 services
     - Risks & mitigations (Python 3.12, distroless Go, Az CLI on Azure Linux)
     - Expected outcomes and rollback plan
     - Implementation details for each service type

### Verification

- Local docker/podman unavailable in sandbox → deferred to ACR build
- All MCR images verified available via MCR API
- No breaking changes expected (verified dependencies, static Go binary, etc.)

### Next Steps for Reviewer/Deployer

1. Review Dockerfiles for correctness
2. Run `task cloud:deploy` to rebuild services
3. Monitor ACR build logs for all 5 services
4. If any service fails: check ACR task logs for detailed error
5. If distroless Go runtime fails: Turk documented fallback to `mcr.microsoft.com/azurelinux/base/core:3.0`

