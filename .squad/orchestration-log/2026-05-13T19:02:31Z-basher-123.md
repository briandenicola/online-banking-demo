# Orchestration Log — Basher — Issue #123

**Timestamp:** 2026-05-13T19:02:31Z  
**Agent:** Basher (Backend Dev — .NET/Redis)  
**Issue:** #123 — AI dashboard tiles stuck at 0 (re-scoring + AI Calls counter)  
**Status:** ✅ SUCCESS  
**Branch:** squad/p2-wave-3  
**Commit:** c241a18  

## Summary

Fixed two root causes behind the AI dashboard tiles (Avg Risk Score, Total Scored, AI Calls Today) stuck at 0:

1. **Dead consumer task**: `consume_redis_stream()` exception on `xgroup_create()` BUSYGROUP (uncaught after first run) → task died before scoring loop. Two-line guard added.
2. **Lost historical transactions**: New admin endpoint `POST /api/admin/replay-events?limit=N` on transaction-service drains Cosmos backlog and re-publishes as events.

## Scope
- **Files:** 
  - `src/ai-service/` (Redis consumer guard, 2 lines)
  - `src/transaction-service/` (replay endpoint)
- **Duration:** 2289s
- **Outcome:** Issue #123 closed. Live recovery verified: avgRiskScore 0.27, totalScored 84+, aiCallsToday 17–68 (per-pod).

## Verification
```
before: avgRiskScore=0.00, totalScored=0,  aiCallsToday=0
after : avgRiskScore=0.27, totalScored=84+, aiCallsToday=17/68
flagged: 27 → 44 (high-risk replays caught)
```

## Follow-ups (filed as separate issues)
1. `aiCallsToday` per-pod flickering (should be Redis-backed `INCR` against daily key)
2. No DLQ visibility if consumer dies silently again (enhance `/readyz`)
3. `xreadgroup` slow for backfills (took ~12min for 155 events)

## Decision Drops
- Files: `.squad/decisions/inbox/basher-123-dashboard-zeros.md` (primary), `.squad/decisions/inbox/basher-accounts-regression.md` (related Cosmos casing fix for #121/#125, to be merged into decisions.md)
