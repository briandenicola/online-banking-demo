# Orchestration Log Entry

2026-05-13T21:52:18Z — Basher (Backend Dev) — RCA + repin agent-framework SDKs

| Field | Value |
|-------|-------|
| **Agent routed** | Basher (Backend Dev) |
| **Why chosen** | Issue #137: eval-403 trace required Python service expertise; SDK versioning and pyproject.toml management within backend domain |
| **Mode** | `background` |
| **Why this mode** | Non-blocking RCA; decision artifacts could be produced independently of user approval |
| **Files authorized to read** | src/ai-service/pyproject.toml, src/chatbot-service/pyproject.toml, src/account-opening-service/pyproject.toml, GH issue #137, container build logs (inferred context) |
| **Files agent must produce** | src/ai-service/pyproject.toml (repinned), src/chatbot-service/pyproject.toml (repinned), src/account-opening-service/pyproject.toml (repinned), .squad/decisions/inbox/basher-eval403-repin.md (decision record), GH issue #137 (updated) |
| **Outcome** | **SUCCESS** — RCA complete. Root cause: unpinned preview SDKs (`agent-framework-*="*"`) pulling PyPI 1.3.0 (2026-05-08) with breaking eval contract. Pinned to 1.2.2 / 1.0.0b9. Commit 0b6255a staged. Decision record created with exact-pin convention for future preview-SDK upgrades. |

---

## Summary

**Exact-Pin Preview SDK Convention Established**

Agent-framework-core, agent-framework-foundry, and azure-ai-inference preview channels require exact pins (e.g., `"1.2.2"`) due to frequent breaking changes between beta releases—unlike stable libs that use range constraints (`>=min,<next-major`). Applies to:
- src/ai-service/pyproject.toml
- src/chatbot-service/pyproject.toml
- src/account-opening-service/pyproject.toml
- budget-service unchanged (no agent-framework dependency)

**Remediation:** CI lint rule to prevent `agent-framework.*="*"` regressions.
