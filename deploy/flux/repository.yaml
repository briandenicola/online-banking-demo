apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: banking-demo
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/${GITHUB_REPOSITORY_OWNER}/${GITHUB_REPOSITORY_NAME}.git
  ref:
    branch: main