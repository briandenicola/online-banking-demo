version: '3'

vars:
  DOCKER_TAG: '{{default "latest" .CLI_ARGS}}'

tasks:
  default:
    desc: Build all container images using Azure ACR
    cmds:
      - task: user-service
      - task: account-service
      - task: account-opening-service
      - task: transaction-service
      - task: transfer-service
      - task: prompt-eval-service
      - task: chatbot-service
      - task: budget-service
      - task: ai-service
      - task: event-processor
      - task: authority-service
      - task: banker-copilot-service
      - task: ui-app

  user-service:
    desc: Build user-service container image
    dir: '{{.ROOT_DIR}}'
    cmds:
      - az acr build --registry {{.ACR_NAME}} --image user-service:{{.DOCKER_TAG}} -f ./src/user-service/Dockerfile .

  account-service:
    desc: Build account-service container image
    dir: '{{.ROOT_DIR}}'
    cmds:
      - az acr build --registry {{.ACR_NAME}} --image account-service:{{.DOCKER_TAG}} -f ./src/account-service/Dockerfile .

  account-opening-service:
    desc: Build account-opening-service container image
    dir: '{{.ROOT_DIR}}'
    cmds:
      - az acr build --registry {{.ACR_NAME}} --image account-opening-service:{{.DOCKER_TAG}} ./src/account-opening-service/

  transaction-service:
    desc: Build transaction-service container image
    dir: '{{.ROOT_DIR}}'
    cmds:
      - az acr build --registry {{.ACR_NAME}} --image transaction-service:{{.DOCKER_TAG}} -f ./src/transaction-service/Dockerfile .

  transfer-service:
    desc: Build transfer-service container image
    dir: '{{.ROOT_DIR}}'
    cmds:
      - az acr build --registry {{.ACR_NAME}} --image transfer-service:{{.DOCKER_TAG}} -f ./src/transfer-service/Dockerfile .

  prompt-eval-service:
    desc: Build prompt-eval-service container image
    dir: '{{.ROOT_DIR}}'
    cmds:
      - az acr build --registry {{.ACR_NAME}} --image prompt-eval-service:{{.DOCKER_TAG}} -f ./src/prompt-eval-service/Dockerfile .

  chatbot-service:
    desc: Build chatbot-service container image
    dir: '{{.ROOT_DIR}}'
    cmds:
      - az acr build --registry {{.ACR_NAME}} --image chatbot-service:{{.DOCKER_TAG}} ./src/chatbot-service/

  budget-service:
    desc: Build budget-service container image
    dir: '{{.ROOT_DIR}}'
    cmds:
      - az acr build --registry {{.ACR_NAME}} --image budget-service:{{.DOCKER_TAG}} ./src/budget-service/

  ai-service:
    desc: Build ai-service container image
    dir: '{{.ROOT_DIR}}'
    cmds:
      - az acr build --registry {{.ACR_NAME}} --image ai-service:{{.DOCKER_TAG}} ./src/ai-service/

  # Epic #332. Both Copilot images take the REPOSITORY ROOT as build context, not
  # their own directory: each copies shared configuration from outside its
  # service folder (config/authority-policy.yaml, config/copilot-tools.yaml, and
  # src/user-service/config/role-hierarchy.yaml). A service-directory context
  # would fail to find them, or worse, silently bake a stale copy.
  #
  # authority-service was missing from this file after Phase 1 — the manifests
  # and the identity landed but nothing ever built the image, so a cloud deploy
  # would have reached ImagePullBackOff. Added here with its Phase 2 sibling.
  authority-service:
    desc: Build authority-service container image (Banker Copilot approval executor)
    dir: '{{.ROOT_DIR}}'
    cmds:
      - az acr build --registry {{.ACR_NAME}} --image authority-service:{{.DOCKER_TAG}} -f ./src/authority-service/Dockerfile .

  banker-copilot-service:
    desc: Build banker-copilot-service container image (Banker Copilot agent harness)
    dir: '{{.ROOT_DIR}}'
    cmds:
      - az acr build --registry {{.ACR_NAME}} --image banker-copilot-service:{{.DOCKER_TAG}} -f ./src/banker-copilot-service/Dockerfile .

  eval-debug:
    desc: Build eval-debug container image (ai-service code + az CLI for in-cluster eval debugging — issue #145)
    dir: '{{.ROOT_DIR}}'
    cmds:
      - az acr build --registry {{.ACR_NAME}} --image eval-debug:{{.DOCKER_TAG}} -f ./src/ai-service/Dockerfile.eval-debug ./src/ai-service/

  event-processor:
    desc: Build event-processor container image
    dir: '{{.ROOT_DIR}}'
    cmds:
      - az acr build --registry {{.ACR_NAME}} --image event-processor:{{.DOCKER_TAG}} ./src/event-processor/

  ui-app:
    desc: Build ui-app container image
    dir: '{{.ROOT_DIR}}'
    cmds:
      - az acr build --registry {{.ACR_NAME}} --image ui-app:{{.DOCKER_TAG}} ./src/ui-app/
